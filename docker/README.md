# Optional container profile

The application is developed and verified with native Windows PowerShell commands. Container support is optional and will be documented separately when PostgreSQL, Neo4j, and background workers are introduced.

Do not assume Docker is installed on the target machine. The current setup script intentionally does not require it.
